Read `AGENTS.md` first: it holds the repo rules (layout, two products, commit style, checks).
`CONTRACTS.md` fixes module boundaries and the browser protocol; `README.md` explains the tool.
Run `pnpm check && pnpm test:smoke` before any commit; never add a co-author trailer.
